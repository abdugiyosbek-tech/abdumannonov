{
  "name": "fit-jarvis-portal",
  "version": "3.0.0-mvp",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python -m http.server 8000"
  }
}
